# 明日测试计划

> **日期**：2026-03-14
**目标**：完成阶段 3（测试阶段），验证 v0.2.0 的所有改进

---

## 📋 测试目标

### 核心验证目标

1. **验证路径控制 100% 有效**
   - 所有文件操作都必须使用 `.learn-system/` 前缀
   - 模型不得使用其他路径
   - 验证强制性规则被模型遵守

2. **验证数据结构正确**
   - `goals.json` 格式正确
   - `progress.json` 格式正确
   - `bookmarks.json` 格式正确
   - 所有 JSON 文件可以被正确解析

3. **验证所有功能正常工作**
   - 创建学习目标
   - 查看学习状态
   - 创建书签
   - 查看书签列表
   - 更新进度

4. **评估整体评分**
   - 预期评分 ≥80/100（发布标准）
   - 优先级最高：功能完整性和准确性

---

## 🧪 测试用例

### 核心功能测试（优先：高）

#### 测试 1：路径控制验证（10 个子测试）

**测试组**：test-path-control-v2
**执行方式**：创建多个书签和目标，验证路径

| 测试 | 预期结果 | 验证方法 |
|------|---------|----------|
| 创建书签 A | 保存到 `.learn-system/bookmarks.json` | 检查文件路径 |
| 创建书签 B | 保存到 `.learn-system/bookmarks.json` | 检查文件路径 |
| 创建目标 A | 保存到 `.learn-system/goals.json` | 检查文件路径 |
| 创建目标 B | 保存到 `.learn-system/goals.json` | 检查文件路径 |
| 查看状态 | 读取 `.learn-system/progress.json` | 检查文件路径 |
| 更新进度 | 更新 `.learn-system/progress.json` | 检查文件路径 |

---

#### 测试 2：强制性规则验证（5 个子测试）

**测试组**：test-mandatory-rules-v2
**执行方式**：尝试违反规则，验证是否被阻止

| 测试 | 预期结果 | 验证方法 |
|------|---------|----------|
| 尝试在根目录创建文件 | 被拒绝或报错 | 检查是否在根目录创建了文件 |
| 尝试使用绝对路径 | 被拒绝或报错 | 检查是否使用了绝对路径 |
| 尝试使用 `~/` 符号 | 被拒绝或报错 | 检查是否使用了 `~/` 符号 |
| 尝试使用旧路径结构 | 被拒绝或报错 | 检查是否创建了 `.learn-system/` 目录 |
| 跳过目录创建 | 被拒绝或报错 | 检查模型是否执行了 `mkdir -p` |

---

#### 测试 3：数据结构验证（3 个子测试）

**测试组**：test-data-structure-v2
**执行方式**：验证 JSON 文件格式和内容

| 测试 | 预期结果 | 验证方法 |
|------|---------|----------|
| 验证 goals.json | JSON 格式正确，字段完整 | 解析 JSON，检查所有必需字段 |
| 验证 progress.json | JSON 格式正确，字段完整 | 解析 JSON，检查所有必需字段 |
| 验证 bookmarks.json | JSON 格式正确，字段完整 | 解析 JSON，检查所有必需字段 |

---

#### 测试 4：功能完整性测试（10 个子测试）

**测试组**：test-functionality-v2
**执行方式**：测试所有核心功能

| 测试 | 输入 | 预期结果 |
|------|------|----------|
| 创建学习目标 | "我要学 X" | 创建成功，保存在 `.learn-system/goals.json` |
| 查看学习目标 | "查看学习目标" | 显示所有目标，从 `.learn-system/goals.json` 读取 |
| 创建书签 | "创建书签 Y" | 创建成功，保存在 `.learn-system/bookmarks.json` |
| 查看书签 | "查看我的书签" | 显示所有书签，从 `.learn-system/bookmarks.json` 读取 |
| 查看学习状态 | "查看学习状态" | 显示状态，从 `.learn-system/progress.json` 读取 |
| 更新进度 | "更新进度，完成模块 A" | 更新成功，更新 `.learn-system/progress.json` |
| 完成书签 | "完成书签 Z" | 更新成功，更新 `.learn-system/bookmarks.json` |
| 切换目标 | "切换学习目标" | 切换成功，更新 `.learn-system/goals.json` |
| 暂停目标 | "暂停学习目标" | 暂停成功，更新 `.learn-system/goals.json` |
| 恢复目标 | "恢复学习目标" | 恢复成功，更新 `.learn-system/goals.json` |

---

## 📊 测试执行计划

### 时间安排

**开始时间**：2026-03-14 上午 9:00
**预计用时**：4-6 小时

### 执行顺序

1. **09:00-10:00**：测试 1（路径控制）- 10 个子测试
2. **10:00-11:00**：测试 2（强制性规则）- 5 个子测试
3. **11:00-12:00**：测试 3（数据结构）- 3 个子测试
4. **12:00-14:00**：测试 4（功能完整性）- 10 个子测试
5. **14:00-15:00**：测试结果分析和报告生成
6. **15:00-15:30**：问题修复（如果需要）
7. **15:30-16:00**：最终测试和评分

---

## 📊 评分标准

### 详细评分表

| 维度 | 权重 | 满分 | 得分标准 |
|------|------|------|----------|
| **功能完整性** | 40% | 40 | 所有核心功能正常工作，无重大缺陷 |
| **准确性** | 30% | 30 | 意图识别准确，无严重误判或误触发 |
| **性能** | 15% | 15 | 响应时间合理（<3分钟/操作），token 使用高效 |
| **兼容性** | 10% | 10 | 与现有系统兼容，无破坏性变更 |
| **用户体验** | 5% | 5 | 路径控制有效，用户体验良好 |

### 总分计算

```
总分 = (功能完整性得分 × 40%) + (准确性得分 × 30%) + (性能得分 × 15%) + (兼容性得分 × 10%) + (用户体验得分 × 5%)
```

### 发布标准

- **发布**：≥80/100
- **不发布**：<80/100

---

## 📋 检查清单

### 测试前

- [ ] 创建测试目录 `test-v0.2.0-final/`
- [ ] 确保 v0.2.0 已更新到 OpenClaw
- [ ] 准备测试数据和环境

### 测试中

- [ ] 记录每个测试的执行结果
- [ ] 收集错误日志和异常
- [ ] 验证文件路径和数据结构
- [ ] 测量响应时间和 token 使用

### 测试后

- [ ] 分析所有测试结果
- [ ] 计算各维度得分
- [ ] 生成测试报告
- [ ] 评估是否达到发布标准
- [ ] 列出需要修复的问题（如果有）

---

## 🎯 成功标准

### 路径控制

- ✅ 所有文件操作都使用 `.learn-system/` 前缀
- ✅ 没有任何文件在其他位置创建
- ✅ 模型严格遵守强制性规则

### 数据结构

- ✅ 所有 JSON 文件格式正确
- ✅ 所有必需字段都存在
- ✅ JSON 文件可以被正确解析和操作

### 功能完整性

- ✅ 至少 80% 的核心功能测试通过
- ✅ 没有重大功能缺陷

### 评分

- ✅ 总分 ≥80/100
- ✅ 功能完整性 ≥32/40
- ✅ 准确性 ≥24/30

---

## 🚀 准备就绪

### 测试环境

- ✅ 版本：v0.2.0（开发版本）
- ✅ 目录：`.learn-system/` 项目级管理
- ✅ 结构：JSON 简化数据结构
- ✅ 规则：强制性路径控制

### 预期结果

- **评分**：≥80/100（发布标准）
- **路径控制**：100% 有效
- **数据结构**：JSON 格式正确
- **功能完整性**：核心功能正常工作

---

**明天上午 9:00 开始测试** 🎯

*Created: 2026-03-13*
